build:
  docker:
    web-client: client/Dockerfile
    web-server: server/Dockerfile

release:
  image: web-client
  command:
    - echo "Deployed!!!"

run:
  web-client: npm start
  web-server: npm start
